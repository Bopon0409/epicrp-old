import { observer } from "mobx-react-lite";
import './scss/sections.scss'



export const Sections = observer(( state ) => {
    return(
        <div className='sections'>
            {
            }
        </div>
    )
})